# Contexta Agent — dipendenze Python

# Core
httpx>=0.27
python-dotenv>=1.0
pyspellchecker>=0.8
nltk>=3.8

# EPUB text extraction (per-book knowledge base)
EbookLib>=0.18
beautifulsoup4>=4.12

# Server locale
fastapi>=0.111
uvicorn[standard]>=0.30
orjson>=3.9
aiofiles>=23.0
python-multipart>=0.0.9

# Provider SDK — installare solo quello usato (LLM_PROVIDER nel .env)
anthropic>=0.25     # LLM_PROVIDER=anthropic
# openai>=1.30      # LLM_PROVIDER=openai
